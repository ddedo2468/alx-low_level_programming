#include <stdio.h>
#include "main.h"
/**
 * mul - multi 2 numbers
 * @a: first num
 * @b: second num
 * Return: result of multi
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
