#include "main.h"

/**
 * _isdigit - check if it's a digit
 * Description: telss yot if it's digit or not
 * @c: int to be checked.
 * Return: 1 if digit else 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	
		return(1);

	return (0);
}
