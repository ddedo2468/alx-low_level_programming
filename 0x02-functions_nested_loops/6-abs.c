#include "main.h"
/*
 * _abs - computes absolute value of a number
 * @n: the number wich will be checked
 * Return: abs value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
