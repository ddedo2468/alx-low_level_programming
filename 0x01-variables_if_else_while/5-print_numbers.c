#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point to program.
 * Return: 0
 */
int main(void)
{
    int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');

	return (0);
}
